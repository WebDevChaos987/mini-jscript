# mini-jscript
Welcome to Mini-Jscript. A site that allows you to run Javascript directly inside the browser. When doing math though; you cannot directly type 1+1. You have to directly tell it to display 1+1, e.g. console.log(1+1);
